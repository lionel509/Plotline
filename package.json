{
  "name": "plotline",
  "version": "0.2.0",
  "description": "Obsidian plugin: a Desmos-style graphing calculator for notes.",
  "main": "main.js",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "check": "tsc --noEmit",
    "install-local": "node esbuild.config.mjs && node install.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.24.0",
    "obsidian": "^1.7.2",
    "typescript": "^5.7.2"
  }
}
